## 2024-06-22 - Primary Action Button Variants
**Learning:** In Gradio interfaces with multiple buttons side-by-side, users may accidentally click secondary actions (like "Clear") when trying to execute primary actions (like "Run") due to identical visual weight.
**Action:** Use `variant="primary"` for the main action button in a button row to establish clear visual hierarchy.
