## 2026-05-24 - Add primary variant to Gradio actions
**Learning:** When building Gradio interfaces, always assign `variant='primary'` to main action buttons (e.g., 'Run', 'Authenticate') when they are placed near secondary buttons to establish clear visual hierarchy and prevent accidental clicks.
**Action:** Assign `variant='primary'` to main action buttons.
