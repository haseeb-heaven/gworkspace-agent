# Configuration Schema

This file defines the configuration schema for the GitHub PR Monitor skill.

## Parameters

- `pr_url`: The URL of the GitHub PR to monitor.
- `interval`: Polling interval in seconds (default: 300).
